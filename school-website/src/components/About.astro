---
import AboutContent from './react/AboutContent.jsx';
---

<section id="about" class="py-12 lg:py-28 bg-gray-50 relative overflow-hidden">
  <div class="absolute inset-0 flex items-center justify-center pointer-events-none select-none overflow-hidden">
    <img src={(import.meta.env.BASE_URL + '/logo.png').replace('//', '/')} alt="" class="w-[140px] sm:w-[300px] lg:w-[500px] 2xl:w-[800px] opacity-10 object-contain" aria-hidden="true" />
  </div>
  <div class="absolute top-0 right-0 w-40 sm:w-64 lg:w-96 h-40 sm:h-64 lg:h-96 bg-amber-50 rounded-full blur-3xl"></div>
  <div class="absolute bottom-0 left-0 w-40 sm:w-64 lg:w-96 h-40 sm:h-64 lg:h-96 bg-teal-50 rounded-full blur-3xl"></div>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="scroll-reveal-item text-center mb-14 lg:mb-18">
      <span class="inline-block px-4 py-1.5 bg-amber-50 text-amber-700 font-semibold text-sm rounded-full mb-4 border border-amber-100">
        About Us
      </span>
      <h2 class="text-3xl lg:text-4xl xl:text-5xl font-bold text-gray-900 mb-4">
        A Home for Learning <span class="text-amber-600">Since 2011</span>
      </h2>
      <p class="text-lg text-gray-600 max-w-3xl mx-auto leading-relaxed">
        At Mother's Care High School, we believe education is not just about textbooks and exams. It is about nurturing curious minds, building strong character, and creating a warm, supportive environment where every child feels valued. Our English Medium curriculum, dedicated faculty, and child-first approach make us the trusted choice for parents in Adilabad.
      </p>
    </div>

    <AboutContent client:load />
  </div>
</section>
